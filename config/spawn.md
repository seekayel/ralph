---
command: bun
args: []
---

# Spawn Configuration

This step is handled internally by Ralph and does not invoke an external agent.
It creates the git worktree and runs setup commands from README.md.
